# Handover - Alpha

## Estado
Alpha completó su parte de Ola 1:
- Modelos de Asset, Timeline, Price History
- Organizations, Parties, Persons
- Project + Units (recién integrado)
- Migraciones versionadas
- Seeds sintéticos

## Endpoints
- `GET /public/assets`
- `GET /public/assets/:id`
- `GET /public/projects`
- `GET /public/projects/:id`
- `POST /assets` (JWT ADMIN)
- `POST /projects` (JWT ADMIN)
- `PATCH /assets/:id/status` (JWT ADMIN)

## Repo
https://github.com/Sthefano22/ZENPLUS